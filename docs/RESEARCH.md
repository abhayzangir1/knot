# Knot platform research register

**Last reviewed:** 2026-07-13
**Rule:** A verified fact is tied to a current primary source. Anything that
depends on a Slack workspace, administrator, account, or future platform change
is explicitly marked uncertain and must be capability-checked rather than
assumed.

## Verified constraints

| Topic | Current primary source | Verified fact | Knot decision |
| --- | --- | --- | --- |
| Hackathon eligibility and deadline | [Slack Agent Builder Challenge rules](https://slackhack.devpost.com/rules) | The deadline is July 13, 2026 at 5:00 p.m. PDT. Judging runs from July 14 through August 6, and winners are scheduled to be announced on or around August 11, 2026. A submission must use at least one of Slack AI capabilities, MCP server integration, or the Real-Time Search API. The New Slack Agent track requires a working-project demo, architecture diagram, developer-sandbox URL, reviewer access for `slackhack@salesforce.com` and `testing@devpost.com`, and a comprehensive demonstration video under three minutes on YouTube, Vimeo, Facebook Video, or Youku. The video must show the functioning project and contain no confidential information, unauthorized copyrighted material, or unauthorized third-party trademarks. The track does not state the Marketplace requirement applied to the Organizations track. Submission materials must be in English or include an English translation. | Target the New Slack Agent track. Knot will qualify through a real Knot Core MCP integration and Slack-native agent behavior only after the walking skeleton passes; Phase 1 alone is not an eligible submission and must not be presented as one. Keep the submitted deployment and sandbox accessible through the winner announcement, and reserve time for the public demo video and final submission receipt rather than treating deployment as the final step. |
| Slack acknowledgement | [Bolt JS acknowledgement](https://docs.slack.dev/tools/bolt-js/concepts/acknowledge/) | Slack actions, commands, and options requests must be acknowledged; Slack recommends acknowledgement before database or network work because the response window is three seconds. | Verify/minimally validate/deduplicate, optionally record a tiny ingress receipt, then acknowledge before slow work. Release budget is p95 <500 ms, p99 <1 s, zero sandbox timeout. |
| Installed workspace identity | [auth.test](https://docs.slack.dev/reference/methods/auth.test) | A successful bot-token authentication response includes `team_id` and the authenticated bot identity. | Knot calls `auth.test` at startup and rejects every signed interaction whose team ID differs. This intentionally supports one workspace in Phase 1; multi-workspace OAuth remains blocked. |
| Slack signed ingress | [Verify requests from Slack](https://docs.slack.dev/authentication/verifying-requests-from-slack/) | Slack sends a signature over the raw body and a timestamp; its guidance shows rejecting requests older than five minutes and recommends constant-time comparison. | Preserve raw body, verify HMAC signature before parsing, enforce replay window, and reject forged/replayed input. |
| Recoverable app DM delivery | [chat.postMessage](https://docs.slack.dev/reference/methods/chat.postMessage) and [conversations.history](https://docs.slack.dev/reference/methods/conversations.history) | `chat.postMessage` supports an `event_type`/`event_payload` metadata object; Slack warns that metadata is visible to workspace apps/users. `conversations.history` can return metadata, and some new non-Marketplace commercial distributions are limited to one history request per minute and 15 objects. | Store only an opaque, non-sensitive delivery ID in metadata. Do not read history on the first attempt; on an ambiguous durable retry, scan at most 15 recent app-DM messages and suppress a matching duplicate. |
| Direct-message safety reads | [conversations.history](https://docs.slack.dev/reference/methods/conversations.history) and [im:history](https://docs.slack.dev/reference/scopes/im.history/) | Reading direct-message history requires the matching history scope; `im:history` is supported for bot tokens and covers direct messages. | Knot requests `im:history` for two bounded reads: compare the app-owned canonical card before rollback, and scan recent metadata only after an ambiguous private-message delivery retry. First attempts do not read history; private-channel create/invite scopes remain absent. |
| Slack-normalized app-card before-state | Stable-host sandbox execution trace on 2026-07-14, using the app-owned message returned by `conversations.replies` | Slack returned a semantically equivalent but structurally normalized Block Kit payload after Knot posted the canonical card. Comparing it with the pre-post request body produced a false stale-state conflict. This is observed target-workspace behavior, not a claim that every Slack message is rewritten. | Before planning an app-card update, fetch the exact current message, verify its channel, timestamp, and stored audience binding, and bind the immutable plan to Slack's returned blocks and fallback text. Continue to fetch and compare again at execution and rollback so any post-preview change fails closed. |
| Human role validation | [users.info](https://docs.slack.dev/reference/methods/users.info/) and [users:read](https://docs.slack.dev/reference/scopes/users.read/) | Slack documents `users.info` for bot tokens with `users:read`; the returned user object identifies deleted, bot, and app users. Email access requires a separate scope and is unnecessary here. | Knot requests `users:read` only to reject invalid owner, next-move, and reviewer assignments before creating an outcome. It does not request or process user email addresses. |
| Agent View | [Developing an agent](https://docs.slack.dev/ai/developing-agents/) | New apps use the Agent messaging experience, which uses agent_view and app_home_opened, app_context_changed, and message.im events. Some AI features require a paid plan, while Slack says a Developer Program sandbox can be fully featured. | Treat Agent View as a supported surface after the skeleton; DMs, shortcut, modal, and card flows must remain functional without it. |
| Slackbot MCP client | [Slackbot MCP Client](https://docs.slack.dev/ai/slackbot-mcp-client/) and [June 2026 announcement](https://docs.slack.dev/changelog/2026/06/18/slackbot-mcp-client/) | Slack publicly announced the client, but its current implementation guide still says access is rolling out and directs developers to detect the MCP Servers entry in App Settings. Slack identity auth sends signed requests and provides the Slack user/team identity in `_meta.slack`; tool calls also receive Slackbot's explicit user-authorization UI. Actual app-setting visibility, installation, and administrator approval therefore vary by workspace. | Optional, capability-detected adapter only. The target Knot app displayed MCP Servers on 2026-07-13, so it may be configured after Phase 1. Other installations remain fully usable without it. State-aware tools require Slack identity auth; no-auth mode may not expose outcome data. |
| Phase-1 app manifest | [App manifest reference](https://docs.slack.dev/reference/app-manifest/), [shortcuts guide](https://docs.slack.dev/interactivity/implementing-shortcuts/), [commands scope](https://docs.slack.dev/reference/scopes/commands/), [users.info](https://docs.slack.dev/reference/methods/users.info/), and [im:history](https://docs.slack.dev/reference/scopes/im.history/) | Slack requires `commands` for message and global shortcuts. App manifests support a read-only Messages tab. `users.info` uses `users:read`; direct-message history methods support bot `im:history`. Agent View is an optional separate feature. | Phase 1 uses exactly `chat:write`, `commands`, `im:write`, `im:history`, and `users:read`; Messages is read-only, Home is off, and Agent experience remains off until a complete conversational surface exists. Private-channel management scopes remain absent because Knot creates no outcome channels. |
| Slack token rotation | [Using token rotation](https://docs.slack.dev/authentication/using-token-rotation/) | Slack documents enabling rotation with `settings.token_rotation_enabled: true`, exchanging long-lived credentials, refreshing expiring credentials, and revocation behavior. Revoking a token on a non-rotating app usually revokes that authorization and requires reinstall. | The current manually installed, single-workspace skeleton keeps rotation off only as a controlled Phase-1 boundary. Public or multi-workspace distribution is blocked until Knot implements and tests the installation, refresh, encrypted storage, revocation, and reinstall lifecycle. |
| Container runtime hardening | [Docker build best practices](https://docs.docker.com/build/building/best-practices/) | Docker recommends trusted small bases, explains that tags are mutable and digests provide reproducible pinning, and recommends switching to a non-root `USER` when privileges are unnecessary. Digest pins need an explicit update process so security fixes are not missed. | Knot pins its Node and PostgreSQL base images by digest and runs the application image as the non-root `node` user. Pins are checked during dependency/security refreshes rather than treated as permanently current. |
| Bounded Render packaging | [Docker on Render](https://render.com/docs/docker), [Render health checks](https://render.com/docs/health-checks), and [Render regions](https://render.com/docs/regions) | Render can build a repository Dockerfile, supports HTTP health-check paths, and offers an Ohio region. A checked-in Blueprint does not prove that an account-side service, database, or domain exists or is healthy. | The Blueprint deploys only the current Phase-1 receiver as one Ohio Free web service, uses `/healthz` for liveness, and adds no product capability. Live account configuration, secrets, external database, public URL, and readiness evidence remain mandatory and uncertain. |
| Render Free deployment boundary | [Render Free instances](https://render.com/docs/free), [Render regions](https://render.com/docs/regions), and [Blueprint YAML reference](https://render.com/docs/blueprint-spec) | Render supports `plan: free` for web services and `region: ohio`, but explicitly says Free instances are not for production. A Free web service spins down after 15 idle minutes and takes about one minute to wake. Render Free PostgreSQL expires after 30 days. Live Blueprint validation on 2026-07-13 additionally rejected `maxShutdownDelaySeconds` for a Free web service. | Pin only the Ohio web service to `free`; tests reject paid plans, a Blueprint database, embedded credentials, and Free-incompatible shutdown-delay configuration. Use a non-expiring external Free PostgreSQL service, monitor liveness, and do not claim an uptime SLA. |
| Neon Free PostgreSQL continuity | [Neon pricing](https://neon.com/pricing), [Scale to Zero](https://neon.com/docs/introduction/scale-to-zero), [Neon regions](https://neon.com/docs/introduction/regions), and [PostgreSQL connection guidance](https://neon.com/docs/connect/query-with-psql-editor) | Neon's Free plan is $0 with no time limit or credit card requirement and currently includes 0.5 GB storage, 100 CU-hours per project per month, 5 GB monthly public transfer, and a limited restore window. Compute sleeps after five inactive minutes and resumes on a query in a few hundred milliseconds. Neon requires TLS, supports standard PostgreSQL clients, and identifies Ohio as `aws-us-east-2`. | Use one PostgreSQL 17 project in AWS US East (Ohio), matching Render. Store the Direct TLS connection string only as Render's secret `DATABASE_URL` because the migrator requires a session-level advisory lock. Keep the application pool bounded, use `/healthz` for keep-warm traffic, use `/readyz` for end-to-end checks, and monitor Free allowances. No sample data or Neon Auth is enabled. |
| External availability probe | [Render uptime best practices](https://render.com/docs/uptime-best-practices) and [UptimeRobot plans](https://uptimerobot.com/pricing/) | Render recommends a third-party external HTTP monitoring probe. UptimeRobot's Free plan currently supports a five-minute monitoring interval, which is shorter than Render Free's 15-minute idle window. A probe improves detection and supplies inbound traffic, but Render may still restart a Free service and provides no production SLA. | Monitor only the public `/healthz` endpoint every five minutes until winners are actually announced. The URL contains no secret or Slack content. Check `/readyz` after alerts and before judge sessions, and do not present the probe as an uptime guarantee. |
| PostgreSQL migration serialization | [PostgreSQL advisory locks](https://www.postgresql.org/docs/current/explicit-locking.html#ADVISORY-LOCKS) | Session-level advisory locks have application-defined meaning and remain held until explicitly released or the session ends. | The migrator takes one named session lock on a dedicated connection and destroys that session in `finally`; concurrent migration and clean-failure tests must remain green. |
| PostgreSQL TLS identity verification | [PostgreSQL SSL support](https://www.postgresql.org/docs/current/libpq-ssl.html) | PostgreSQL documents that `verify-full` validates both the certificate chain and the requested server hostname, recommends it for most security-sensitive environments, and warns that `require` does not provide MITM protection under standard libpq semantics. The live pg 8 runtime additionally warned that its current aliases for `prefer`, `require`, and `verify-ca` will change in the next major version. | Normalize those three pg 8 alias values to explicit `verify-full` before constructing application or migration pools. Preserve unrelated connection parameters and leave connection strings without those modes unchanged. |
| Development dependency advisory | [GitHub Advisory GHSA-67mh-4wv8-2f99](https://github.com/advisories/GHSA-67mh-4wv8-2f99) | The advisory affects `esbuild` through 0.24.2 and specifically concerns its development server's permissive cross-origin behavior; 0.25.0 is listed as patched. The local dependency graph reached the affected package only through the optional `drizzle-kit` CLI. | Knot removed the unused schema-generator/studio CLI and its transitive advisory instead of forcing an incompatible downgrade. Ordered reviewed SQL remains the migration source. Recheck available stable tooling when schema-generation needs change. |
| Runtime-image package advisory | [NVD CVE-2026-12151](https://nvd.nist.gov/vuln/detail/CVE-2026-12151) | Undici versions from 6.17.0 up to 6.27.0 are affected by a high-severity uncontrolled-resource-consumption issue. Docker Scout found the affected 6.26.0 copy only inside the base image's global `npm` CLI, not Knot's production dependency graph. | Install exact production dependencies, then remove the unused global `npm` and `npx` tooling from the final image. The resulting image must pass a fresh critical/high container scan and still start directly with `node`. |
| Slack hosted MCP server | [Slack MCP Server overview](https://docs.slack.dev/ai/slack-mcp-server/) | Slack says only Marketplace-published or internal apps may use its MCP server; unlisted apps are prohibited. | Do not depend on Slack-hosted MCP. It remains absent from unlisted hackathon deployments. |
| Real-Time Search API | [RTS API](https://docs.slack.dev/apis/web-api/real-time-search-api/) | Slack says RTS is available only to directory-published and internal apps; unlisted distributed apps are not allowed. | Do not depend on RTS or persist RTS results. Enable only after a deployment-specific eligibility and consent review. |
| MCP HTTP transport | [MCP Streamable HTTP transport](https://modelcontextprotocol.io/specification/2025-06-18/basic/transports) | Streamable HTTP uses one HTTP endpoint and requires Origin validation; servers should authenticate connections. | Knot Core will use authenticated Streamable HTTP, strict schemas, Origin validation, request limits, and actor-bound service calls after the walking skeleton. |
| Linear OAuth and webhooks | [Linear OAuth 2.0](https://linear.app/developers/oauth-2-0-authentication) and [Linear webhook security](https://linear.app/developers/webhooks) | Linear supports OAuth applications. Its webhook guidance requires raw-body HMAC signature verification, timestamp validation, and documents a unique delivery ID; webhook administration may require workspace-admin or OAuth admin scope. | Linear is the only planned external connector, but it is not built before the skeleton. Its final implementation requires explicit scope review, encrypted credentials, signed ingress, dedupe, receipts, reconciliation, and version-checked compensation. |

## Uncertainties requiring capability checks

| Uncertainty | Why it matters | Required resolution |
| --- | --- | --- |
| Slackbot MCP option appears in the target app's App Settings | Controls whether an optional Slackbot MCP adapter can be configured | Check the target developer app at implementation time. If absent, do not expose or simulate the feature. |
| Agent View/AI entitlement in the target sandbox | Controls whether Agent View can be demonstrated | Confirm within the Slack Developer Program sandbox. Preserve shortcut/DM/modal/card equivalents regardless. |
| Slack app distribution status at submission | Controls whether RTS or Slack-hosted MCP are legally available | Treat the app as unlisted unless verified otherwise. Do not request/use either capability for the skeleton. |
| Target app still matches the reviewed manifest after dashboard changes | Slack dashboard toggles and reinstallation state can drift from the versioned manifest | Reimport/review `slack.json`, keep Agent experience off, reinstall after scope changes, and capture the final sandbox configuration in Phase-1 evidence. |
| Linear workspace approval and OAuth scopes | Admin policy controls installation and webhook access | Obtain from a real Linear test workspace only after the Slack gate; absent approval means the connector is not shown as connected. |
| Render account configuration and stable domain | The live Free Ohio service and external Neon database are now verified through `https://knot-1pc1.onrender.com`: liveness and readiness return HTTP 200, and signed negative ingress meets the acknowledgement budget. Slack dashboard state, UptimeRobot, billing controls, and the complete state-changing flow remain account-side evidence. | Keep the stable URL in the reviewed manifest, configure the external `/healthz` monitor, update the Slack dashboard, and capture the complete live Slack gate. Do not infer publishability from deployment health alone. |
| Slack credential lifecycle for distribution | The target app currently has token rotation disabled and no OAuth installation/refresh store | Keep the app limited to the controlled Phase-1 workspace. Do not distribute until a reviewed rotation/reinstall design and its tests satisfy D-035. |
| Devpost project form state and final submission acceptance | Login state and form validation are account-specific and can change | Recheck the official project form immediately before submission, grant the two required reviewer accounts sandbox access, and capture the accepted submission receipt before the deadline. |

## Rejected platform assumptions

- Slackbot MCP availability is not inferred from a blog announcement, a
  different workspace, or an old app. The current developer documentation takes
  precedence and requires capability detection.
- Slack's hosted MCP and RTS are not treated as a shortcut around Slack
  distribution restrictions.
- A model's interpretation of Slack content is not treated as an authoritative
  source of ownership, approval, privacy, action success, or closure.
- A successful HTTP dispatch is not treated as a successful external action
  without a provider receipt/reconciliation result.
- A passing local container or checked-in Render Blueprint is not treated as a
  live deployment, a healthy public endpoint, or a passed Slack gate.
- A non-rotating developer token is not treated as a distribution-ready
  credential lifecycle.

## Research refresh policy

Refresh this register before adding a Slack capability, changing the app
manifest/scopes, enabling MCP/RTS, adding a connector, deploying to a new
workspace, or submitting to Devpost. Record changed facts and their decision
impact in DECISIONS.md; do not silently overwrite an earlier conclusion.
